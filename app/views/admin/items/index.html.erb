<h1>商品一覧</h1>
<% @items.each do |item| %>
  <h4>商品id</h4>
  <%= item.id %>
  <h4>商品名</h4>
  <%= link_to item.name, admin_item_path(item.id) %>
  <h4>税抜価格</h4>
  <%= item.price %>
  <h4>ジャンル</h4>
  
  <h4>ステータス</h4>
  <%= item.is_active %>
<% end %>
  