<h1>ジャンル一覧・追加</h1>

<%= form_with model: @genre, url: '/admin/genres'  do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <%= f.submit '新規登録' %>
<% end %>

<%= @genres.each do |genre| %>
  <h4>ジャンル</h4>
  <%= genre.name %>
<% end %>

