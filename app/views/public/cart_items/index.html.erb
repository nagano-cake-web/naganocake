<h2>ショッピングカート</h2>
<div>
  <p>商品</p>
  <p>単価(税込み)</p>
  <p>数量</p>
  <p>小計</p>
  <p></p>
</div>

<div>
  <div>
    <%= image_tag item.get_image %>
    <%= @cart_items.name %>
  </div>
  <div>
    <%= @cart_items.add_tax_price %>
  </div>
  <div>
    <%= form_with model: @cart_item do |f| %>
      <%= 
      <%= f.submit '変更' %>
    <% end %>
  </div>
  <div>
    <%= cart_item.subtotal %>
  </div>
  <div>
    <%= link_to '削除する', path(cart_item.id) %>
  </div>
</div>

<div>
  <%= link_to '買い物を続ける', items_path %>
</div>

<div>
  <p>合計金額</p>
  <% @cart_item.each do |cart_item| %>
  <div>
    <% @total += cart_item.subtotal %>
    <%= @total %>
  </div>
  <%= link_to "情報入力に進む", new_order_path %>
</div>