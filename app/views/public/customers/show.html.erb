<h1>マイページ</h1>
<h4>登録情報</h4>
<%= link_to "編集する", customer_edit_path(current_customer.id) %>

<div>
  氏名<%= @customer.last_name %> <%= @customer.first_name %>
</div>

<div>
  カナ<%= @customer.last_name_kana %> <%= @customer.first_name_kana %>
</div>

<div>
  郵便番号<%= @customer.postal_code %>
</div>

<div>
  住所<%= @customer.address %>
</div>

<div>
  電話番号<%= @customer.telephone_number %>
</div>

<div>
  メールアドレス<%= @customer.email %>
</div>

<h4>配送先<%= link_to "一覧を見る", addresses_path(current_customer.id) %></h4>
<h4>注文履歴<%= link_to "一覧を見る", orders_path(current_customer.id) %></h4>
